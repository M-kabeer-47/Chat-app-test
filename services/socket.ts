const userSockets = new Map();

// Add multiple key-value pairs
userSockets.set("user123", "socketId_A");
userSockets.set("user456", "socketId_B");
userSockets.set("user789", "socketId_C");
console.log(userSockets);
